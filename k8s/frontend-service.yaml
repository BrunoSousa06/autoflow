apiVersion: v1
kind: Service
metadata:
  name: autoflow-frontend-service
spec:
  type: NodePort
  selector:
    app: autoflow-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080