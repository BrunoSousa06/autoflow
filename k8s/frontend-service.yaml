apiVersion: v1
kind: Service
metadata:
  name: autoflow-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: autoflow-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080