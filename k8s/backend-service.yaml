apiVersion: v1
kind: Service
metadata:
  name: autoflow-backend-service
spec:
  type: ClusterIP
  selector:
    app: autoflow-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081