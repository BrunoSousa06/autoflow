apiVersion: v1
kind: Service
metadata:
  name: autoflow-backend-service
spec:
  type: NodePort
  selector:
    app: autoflow-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
      nodePort: 30080
